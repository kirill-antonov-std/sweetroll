# sweetroll

Devcontainer:
    Dockerfile
    devcontainer.json

RPI:
    RPI image
    k3s
    docker
    python3-opencv
    test camera
    udev

RPI_app:
    microservices:
        camera_capture
        cloud_uploader
        api_gateway
        (future) image_recognizer
        (future) video streaming
        (future) health controller
    docker-compose

